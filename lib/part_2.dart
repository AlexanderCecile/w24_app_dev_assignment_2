import 'package:flutter/material.dart';

class Part2Main extends StatelessWidget {
  const Part2Main({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Part 2')), body: Placeholder());
  }
}