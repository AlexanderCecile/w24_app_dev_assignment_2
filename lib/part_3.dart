import 'package:flutter/material.dart';

class Part3Main extends StatelessWidget {
  const Part3Main({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('Part 3'),
        ),
        body: Placeholder());
  }
}
