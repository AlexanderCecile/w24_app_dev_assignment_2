import 'package:flutter/material.dart';

class Part1Main extends StatelessWidget {
  const Part1Main({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('Part 1'),
        ),
        body: Placeholder());
  }
}
